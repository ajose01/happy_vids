<tbody>
  <% users.each do |user| %>
    <tr>
      <td><%= link_to user.id, user %></td>
      <td><%= user.name %></td>
      <td><%= user.created_at %></td>
      <td><%= user.updated_at %></td>
    </tr>
  <% end %>
</tbody>
